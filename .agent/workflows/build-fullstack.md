---
description: Generate full React + Tailwind + Firebase + Stripe integrated production code.
---

When I type /build-fullstack, generate a full production-ready project.

Requirements:
- Frontend: React (Vite) + Tailwind CSS.
- Backend: Firebase Firestore for database.
- Payment: Stripe Checkout integration.
- After payment success:
  - Store order data in Firestore.
  - Redirect user to WhatsApp with prefilled message.
- Include dynamic pricing logic if described.
- Include responsive UI.
- Include clean component structure.
- No placeholder lorem ipsum.
- Output complete code ready for deployment.
