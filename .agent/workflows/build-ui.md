---
description: Generate complete React + Tailwind UI for described sections using clean ecommerce-style design.
---

When I type /build-ui, generate a complete React + Tailwind CSS user interface based on my description.

Rules:
- Use clean minimalist ecommerce-style UI inspired by beminimalist.co.
- White background, deep red (#a00000) accent.
- Large whitespace, card layouts, soft shadows.
- Mobile-first responsive layout.
- Use realistic text content. No lorem ipsum.
- Do NOT include backend or database logic.
- Structure code into reusable React components.
- Include dynamic state logic if UI requires it.
- Output ready-to-run Vite + React code.